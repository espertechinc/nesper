<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFrameworks>net462;net472;netcoreapp3.1;net5.0</TargetFrameworks>
    </PropertyGroup>

    <PropertyGroup>
        <AssemblyName>NEsper.Log.NLog</AssemblyName>
        <AssemblyTitle>NEsper.Log.NLog</AssemblyTitle>
        <PackageId>NEsper.Log.NLog</PackageId>
        <RootNamespace>com.espertech.esper</RootNamespace>
        <Summary>NLog specific logger for NEsper.</Summary>
        <Description>NLog specific logger for NEsper.</Description>
        <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\NEsper.Compat\NEsper.Compat.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="NLog" Version="4.7.5"/>
    </ItemGroup>
</Project>
